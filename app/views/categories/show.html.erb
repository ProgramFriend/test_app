<% @title = @category.name %>
<h1 align="center"><%= "Kategorija: " + @category.name %></h1>

<div align="center">
  <% if logged_in? and current_user.admin %>
    <div class="row">
      <span class="badge hvr-grow"><%= link_to "Keisti kategorijos pavadinimą.", edit_category_path(@category) %></span>
    </div>
  <% end %>
  <%= will_paginate @category_articles %>
</div>
<%= render 'articles/article', obj: @category_articles %>

<div align="center">
  <%= will_paginate @category_articles %>
</div>